name: "Sync labels"

on:
  push:
    branches:
      - main
    paths:
      - .github/tool-configurations/labels.yml
  workflow_dispatch:

permissions: {}

jobs:
  configure-labels:
    name: Configure Labels
    permissions:
      contents: read
      pull-requests: write # For writing labels to the repository
    uses: JackPlowman/reusable-workflows/.github/workflows/common-sync-labels.yml@99407128a38152d1c308f45bf1c3f62209e7067c # v2025.11.23.01
    secrets:
      workflow_github_token: ${{ secrets.GITHUB_TOKEN }}
